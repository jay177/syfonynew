<form method="post" action="{{ path('app_remont_es_delete', {'id': remont_e.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ remont_e.id) }}">
    <button class="btn">Delete</button>
</form>
