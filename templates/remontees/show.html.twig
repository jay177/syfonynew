{% extends 'base.html.twig' %}

{% block title %}Remontees{% endblock %}

{% block body %}
    <h1>Remontees</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ remontee.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ remontee.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ remontee.type }}</td>
            </tr>
            <tr>
                <th>Open</th>
                <td>{{ remontee.open ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Horaire</th>
                <td>{{ remontee.horaire }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ remontee.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_remontees_index') }}">back to list</a>

    <a href="{{ path('app_remontees_edit', {'id': remontee.id}) }}">edit</a>

    {{ include('remontees/_delete_form.html.twig') }}
{% endblock %}
